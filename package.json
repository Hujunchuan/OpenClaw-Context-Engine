{
  "name": "hypergraph-context-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "check": "tsc --noEmit",
    "demo": "node --loader ts-node/esm src/demo.ts",
    "memory:demo": "node --loader ts-node/esm src/layered-memory-demo.ts",
    "compare": "node --loader ts-node/esm src/legacy-vs-hypergraph.ts",
    "demo:snapshots": "node --loader ts-node/esm -e \"import('./fixtures/demo-scenarios.ts').then(async ({ runAllDemoScenarios }) => console.log(JSON.stringify(await runAllDemoScenarios(), null, 2)))\"",
    "runtime:demo": "node --loader ts-node/esm src/adapter-runtime-demo.ts",
    "runtime:validate": "node --loader ts-node/esm src/runtime-validation.ts",
    "openclaw:validate": "node --loader ts-node/esm src/openclaw-main-repo-validation.ts",
    "test": "node --loader ts-node/esm --test tests/**/*.test.ts",
    "plugin:check": "node --loader ts-node/esm -e \"import('./index.ts').then(m => { const regs=[]; m.default({ registerContextEngine(id, factory){ regs.push({ id, factory }); }}); console.log(regs[0]?.id ?? 'no-registration'); })\""
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "peerDependencies": {
    "openclaw": ">=2026.1.26"
  }
}
